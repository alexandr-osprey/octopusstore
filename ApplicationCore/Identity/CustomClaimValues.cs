namespace ApplicationCore.Identity
{
    /// <summary>
    /// Custom claim values to associate with custom claim type and assign to user
    /// </summary>
    public static class CustomClaimValues
    {
        public const string Content = nameof(Content);
    }
}
